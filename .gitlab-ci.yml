test:
  stage: test
  image: python:3.9.4
  script:
    - pip install correctionlib==2.0.0rc4
    - git clone https://cms-nanoAOD:Xi1nyy2QgmDfE9qv2M95@gitlab.cern.ch/cms-nanoAOD/jsonpog-integration.git  -b master cms-nanoAOD-repo --depth 1
    - rm -rf script && cp -r cms-nanoAOD-repo/script .
    - script/testMR.sh
    - echo "TEST_VARIABLE"
    - echo $TEST_VARIABLE
    - echo ".$TEST_VARIABLE."
#    - find . -name *.json | xargs correction validate --version 2

deploy-prod:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."

workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS'
      #      when: never
      #- if: '$CI_COMMIT_BRANCH'
